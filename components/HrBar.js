import React from 'react';
import { View } from 'react-native';

const HrBar = () => {
   return (
      <View
         style={{
            borderBottomColor: '#EEEEEE',
            borderBottomWidth: 1,
         }}
      />
   );
};

export default HrBar;
