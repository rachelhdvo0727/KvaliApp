import React from "react";
import { StyleSheet, Text, View } from "react-native";

export default function ChatRoomScreen(props) {
	return (
		<View>
			<Text>This is one conversation</Text>
		</View>
	);
}

const styles = StyleSheet.create({});
