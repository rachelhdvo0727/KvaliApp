import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const EventDetailsScreen = props => {
   return (
      <View>
         <Text>Event's details</Text>
      </View>
   );
};

export default EventDetailsScreen;

const styles = StyleSheet.create({});
